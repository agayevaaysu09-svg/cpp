int zeroDistance(int a[], int n) {
    int first = -1, last = -1;

    for (int i = 0; i < n; i++)
        if (a[i] == 0) {
            if (first == -1) first = i;
            last = i;
        }

    if (first == -1 || last == -1) return -1; 
    return last - first;
}