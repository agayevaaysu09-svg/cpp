void swapMax(int A[], int B[], int nA, int nB) {
    int maxA = 0, maxB = 0;

    for (int i = 1; i < nA; i++)
        if (A[i] > A[maxA]) maxA = i;

    for (int i = 1; i < nB; i++)
        if (B[i] > B[maxB]) maxB = i;

    swap(A[maxA], B[maxB]);
}