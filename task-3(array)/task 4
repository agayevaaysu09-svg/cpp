void averages(int a[]) {
    double sum = 0, mult = 1;

    for (int i = 0; i < 10; i++)
        sum += a[i];

    for (int i = 10; i < 20; i++)
        mult *= a[i];

    cout << "Ededi orta: " << sum / 10 << endl;
    cout << "Hendesi orta: " << pow(mult, 1.0 / 10) << endl;
}