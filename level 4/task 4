#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i < 1000; i++) {
        int a = i, cem = 0, temp = i;
        while (temp > 0) { int r = temp % 10; cem += r * r * r; temp /= 10; }
        if (cem == a) cout << i << " ";
    }
    return 0;
}