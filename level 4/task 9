#include <iostream>
using namespace std;

int main() {
    int n, cem = 0;
    cin >> n;
    for (int i = 1; i < n; i++) if (n % i == 0) cem += i;
    cout << (cem == n ? "Mukemmel" : "Deyil");
    return 0;
}