#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 1000; i++) {
        int t = i, ters = 0;
        while (t > 0) { ters = ters * 10 + t % 10; t /= 10; }
        if (i == ters) cout << i << " ";
    }
    return 0;
}