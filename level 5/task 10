#include <iostream>
using namespace std;


int bolenSayi(int n) {
    int say = 0;
    for (int i = 1; i <= n; i++) {
        if (n % i == 0)
            say++;
    }
    return say;
}

int main() {
    int n;
    cin >> n;

    cout << bolenSayi(n);

    return 0;
}
